diff -Naur mythtv-test-20190915-g21b9ab7-old/mythtv/libs/libmythtv/mythv4l2m2mcontext.cpp mythtv-test-20190915-g21b9ab7-new/mythtv/libs/libmythtv/mythv4l2m2mcontext.cpp
--- mythtv-test-20190915-g21b9ab7-old/mythtv/libs/libmythtv/mythv4l2m2mcontext.cpp	2019-09-15 13:05:57.000000000 +0200
+++ mythtv-test-20190915-g21b9ab7-new/mythtv/libs/libmythtv/mythv4l2m2mcontext.cpp	2019-08-12 10:54:54.810000010 +0200
@@ -215,7 +215,10 @@
             bool splanarm2m = caps & V4L2_CAP_VIDEO_M2M;
 
             if (!(mplanar || mplanarm2m || splanar || splanarm2m))
+            {
+                LOG(VB_PLAYBACK, LOG_INFO, LOC + QString("Device: %1 No mplanar/mplanarm2m/splanar/splanarm2m found. Skiping this device!").arg(v4l2dev.GetDeviceName()));
                 continue;
+            }
 
             v4l2_buf_type capturetype = V4L2_BUF_TYPE_VIDEO_CAPTURE;
             v4l2_buf_type outputtype = V4L2_BUF_TYPE_VIDEO_OUTPUT;
@@ -258,14 +261,22 @@
                         if (res)
                             break;
                         // this is a bit of a shortcut
-                        if (fdesc.pixelformat == V4L2_PIX_FMT_YUV420 ||
-                            fdesc.pixelformat == V4L2_PIX_FMT_NV12)
+                        if (fdesc.pixelformat == V4L2_PIX_FMT_YUV420  ||
+                            fdesc.pixelformat == V4L2_PIX_FMT_YUV420M ||
+                            fdesc.pixelformat == V4L2_PIX_FMT_NV12    ||
+                            fdesc.pixelformat == V4L2_PIX_FMT_NV12M)
                         {
                             if (!s_supportedV4L2Codecs.contains(codec))
                                 s_supportedV4L2Codecs.append(codec);
                             debug.append(avcodec_get_name(codec));
                             break;
                         }
+                        else
+                        {
+                            LOG(VB_GENERAL, LOG_INFO, LOC + QString("V4L2 disabled as %1 has no YUV420/YUV420M/NV12/NV12M support for %2.")
+                                .arg(v4l2dev.GetDeviceName())
+                                .arg(avcodec_get_name(codec)));
+                        }
                         fdesc.index++;
                     }
                 }
