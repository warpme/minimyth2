diff -Naur mythtv-master-20200324-gaa46cdd-old/mythtv/external/FFmpeg/libavcodec/v4l2_m2m_dec.c mythtv-master-20200324-gaa46cdd-new/mythtv/external/FFmpeg/libavcodec/v4l2_m2m_dec.c
--- mythtv-master-20200324-gaa46cdd-old/mythtv/external/FFmpeg/libavcodec/v4l2_m2m_dec.c	2020-03-24 09:42:31.090000000 +0100
+++ mythtv-master-20200324-gaa46cdd-new/mythtv/external/FFmpeg/libavcodec/v4l2_m2m_dec.c	2020-03-24 09:42:32.286666667 +0100
@@ -137,15 +137,19 @@
     V4L2Context *const output = &s->output;
     int ret;
 
+    if (s->draining)
+        goto dequeue;
+
     if (!s->buf_pkt.size) {
         ret = ff_decode_get_packet(avctx, &s->buf_pkt);
-        if (ret < 0 && ret != AVERROR_EOF)
+        if (ret == AVERROR(EAGAIN))
+            goto dequeue;
+        else if (ret == AVERROR_EOF)
+            return ff_v4l2_context_enqueue_packet(output, &s->buf_pkt); /* null pkt */
+        else if (ret < 0)
             return ret;
     }
 
-    if (s->draining)
-        goto dequeue;
-
     ret = ff_v4l2_context_enqueue_packet(output, &s->buf_pkt);
     if (ret == AVERROR(EAGAIN)) {
         /* no input buffers available, continue dequeing */
