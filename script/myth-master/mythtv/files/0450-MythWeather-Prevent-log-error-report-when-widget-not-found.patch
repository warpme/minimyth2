diff -Naur mythtv-master-20191227-gf15b4f0-old/mythplugins/mythweather/mythweather/weatherScreen.cpp mythtv-master-20191227-gf15b4f0-new/mythplugins/mythweather/mythweather/weatherScreen.cpp
--- mythtv-master-20191227-gf15b4f0-old/mythplugins/mythweather/mythweather/weatherScreen.cpp	2019-12-27 12:19:54.853333333 +0100
+++ mythtv-master-20191227-gf15b4f0-new/mythplugins/mythweather/mythweather/weatherScreen.cpp	2019-12-27 12:19:55.980000001 +0100
@@ -97,15 +97,13 @@
 bool WeatherScreen::prepareScreen(bool checkOnly)
 {
     QMap<QString, QString>::iterator itr = m_dataValueMap.begin();
-    while (itr != m_dataValueMap.end())
+    for ( ; itr != m_dataValueMap.end(); ++itr)
     {
         QString name = itr.key();
         MythUIType *widget = GetChild(name);
 
         if (!widget)
         {
-            LOG(VB_GENERAL, LOG_ERR, "Widget not found " + itr.key());
-
             if (name == "copyright")
             {
                 LOG(VB_GENERAL, LOG_WARNING,
@@ -120,14 +118,17 @@
                         .arg(m_name));
                 return false;
             }
-        }
+            else if (name != "updatetime")
+            {
+                LOG(VB_GENERAL, LOG_ERR, "Widget not found " + itr.key());
+            }
 
-        if( !widget || checkOnly )
-        {
-            ++itr;
             continue;
         }
 
+        if (checkOnly)
+            continue;
+
         if (auto w2 = dynamic_cast<MythUIText *>(widget))
         {
             w2->SetText(itr.value());
@@ -139,7 +140,6 @@
         }
 
         prepareWidget(widget);
-        ++itr;
     }
 
     m_prepared = true;
