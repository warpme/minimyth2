diff -Naur mythtv-master-20150105-g4216e00-old/mythtv/configure mythtv-master-20150105-g4216e00-new/mythtv/configure
--- mythtv-master-20150105-g4216e00-old/mythtv/configure	2015-01-06 16:56:17.752145448 +0100
+++ mythtv-master-20150105-g4216e00-new/mythtv/configure	2015-01-06 16:56:49.158648561 +0100
@@ -6220,6 +6220,7 @@
 
 check_optflags(){
     check_cflags "$@"
+    check_cxxflags "$@"
     enabled lto && check_ldflags "$@"
 }
 
@@ -6227,6 +6228,7 @@
 if enabled lto; then
     test "$cc_type" != "$ld_type" && die "LTO requires same compiler and linker"
     check_cflags  -flto
+    check_cxxflags -flto
     check_ldflags -flto $cpuflags
     disable inline_asm_direct_symbol_refs
 fi
@@ -6472,15 +6474,22 @@
 
 if [ -n "$optflags" ]; then
     add_cflags $optflags
+    add_cxxflags $optflags
 elif enabled small; then
     add_cflags $cflags_size
+    add_cxxflags $cflags_size
 elif enabled optimizations; then
     add_cflags $cflags_speed
+    add_cxxflags $cflags_speed
 else
     add_cflags $cflags_noopt
+    add_cxxflags $cflags_noopt
 fi
 check_cflags -fno-math-errno
+check_cxxflags -fno-math-errno
 check_cflags -fno-signed-zeros
+check_cxxflags -fno-signed-zeros
+
 check_cc -mno-red-zone <<EOF && noredzone_flags="-mno-red-zone"
 int x;
 EOF
