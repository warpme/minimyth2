diff -Naur mednafen-old/src/file.cpp mednafen-new/src/file.cpp
--- mednafen-old/src/file.cpp	2009-05-14 19:12:55.000000000 -0700
+++ mednafen-new/src/file.cpp	2012-04-07 08:14:05.000000000 -0700
@@ -229,7 +229,7 @@
    goto doret;
   }
 
-  while((howmany = gzread(tz, tmp->data + cur_size, cur_alloced - cur_size)) > 0)
+  while((howmany = gzread((gzFile)tz, tmp->data + cur_size, cur_alloced - cur_size)) > 0)
   {
    cur_size += howmany;
    cur_alloced <<= 1;
@@ -282,7 +282,7 @@
  }
  else if(type == MDFN_FILETYPE_GZIP)
  {
-  gzclose(tz);
+  gzclose((gzFile)tz);
  }
  else if(type == MDFN_FILETYPE_ZIP)
  {
@@ -444,7 +444,7 @@
 
    if(!(fceufp = MakeMemWrap(t, 1)))
    {
-    gzclose(t);
+    gzclose((gzFile)t);
     return(0);
    }
 
