diff -Naur mythtv-master-20210225-gc0426e3fea-old/mythtv/libs/libmythbase/mythdb.cpp mythtv-master-20210225-gc0426e3fea-new/mythtv/libs/libmythbase/mythdb.cpp
--- mythtv-master-20210225-gc0426e3fea-old/mythtv/libs/libmythbase/mythdb.cpp	2021-03-17 21:06:54.616666501 +0100
+++ mythtv-master-20210225-gc0426e3fea-new/mythtv/libs/libmythbase/mythdb.cpp	2021-03-17 21:06:57.419999834 +0100
@@ -408,6 +408,11 @@
     if (query.exec() && query.next())
     {
         value = query.value(0).toString();
+
+        LOG(VB_DATABASE, LOG_INFO,
+            QString("GetSetting: DB for:'%1' for host:'%2' returns:'%3'")
+            .arg(key).arg(d->m_localhostname).arg(value));
+
     }
     else
     {
@@ -420,6 +425,11 @@
         if (query.exec() && query.next())
         {
             value = query.value(0).toString();
+
+            LOG(VB_DATABASE, LOG_INFO,
+                QString("GetSetting: no:'%1' found for:'%2'. Asking with NULL hostname returns:'%3'")
+                .arg(key).arg(d->m_localhostname).arg(value));
+
         }
     }
 
