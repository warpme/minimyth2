diff -Naur mythtv-0.27-build-old/mythtv/libs/libmythbase/remotefile.cpp mythtv-0.27-build-new/mythtv/libs/libmythbase/remotefile.cpp
--- mythtv-0.27-build-old/mythtv/libs/libmythbase/remotefile.cpp	2014-07-22 21:21:57.000000000 +0200
+++ mythtv-0.27-build-new/mythtv/libs/libmythbase/remotefile.cpp	2014-07-22 21:26:55.945112409 +0200
@@ -428,8 +428,7 @@
     QString sgroup   = qurl.userName();
     QString host     = qurl.host();
 
-    if (isLocal(url) || (gCoreContext->IsMasterBackend() &&
-        gCoreContext->IsThisHost(host)))
+    if (isLocal(url) || gCoreContext->IsThisBackend(host))
     {
        LOG(VB_FILE, LOG_INFO,
            QString("RemoteFile::Exists(): looking for local file: %1").arg(url));
@@ -1134,8 +1133,7 @@
     if (hostName.isEmpty())
         hostName = gCoreContext->GetMasterHostName();
 
-    if (gCoreContext->IsMasterBackend() &&
-        hostName == gCoreContext->GetMasterHostName())
+    if (gCoreContext->IsThisBackend(hostName))
     {
         StorageGroup sGroup(storageGroup, hostName);
         if (!sGroup.FindFile(filename).isEmpty())
