diff -Naur mythtv-master-20190316-g1a44f54-old/mythplugins/mythweather/mythweather/weatherScreen.cpp mythtv-master-20190316-g1a44f54-new/mythplugins/mythweather/mythweather/weatherScreen.cpp
--- mythtv-master-20190316-g1a44f54-old/mythplugins/mythweather/mythweather/weatherScreen.cpp	2019-03-16 10:54:05.866666667 +0100
+++ mythtv-master-20190316-g1a44f54-new/mythplugins/mythweather/mythweather/weatherScreen.cpp	2019-03-16 10:54:07.923333334 +0100
@@ -101,15 +101,13 @@
 bool WeatherScreen::prepareScreen(bool checkOnly)
 {
     QMap<QString, QString>::iterator itr = m_dataValueMap.begin();
-    while (itr != m_dataValueMap.end())
+    for ( ; itr != m_dataValueMap.end(); ++itr)
     {
         QString name = itr.key();
         MythUIType *widget = GetChild(name);
 
         if (!widget)
         {
-            LOG(VB_GENERAL, LOG_ERR, "Widget not found " + itr.key());
-
             if (name == "copyright")
             {
                 LOG(VB_GENERAL, LOG_WARNING,
@@ -124,14 +122,17 @@
                         .arg(m_name));
                 return false;
             }
-        }
+            else if (name != "updatetime")
+            {
+                LOG(VB_GENERAL, LOG_ERR, "Widget not found " + itr.key());
+            }
 
-        if( !widget || checkOnly )
-        {
-            ++itr;
             continue;
         }
 
+        if (checkOnly)
+            continue;
+
         if (dynamic_cast<MythUIText *>(widget))
         {
             ((MythUIText *) widget)->SetText(itr.value());
@@ -143,7 +144,6 @@
         }
 
         prepareWidget(widget);
-        ++itr;
     }
 
     m_prepared = true;
