diff -Naur mythtv-0.27-20140504-ge2acf0d-old/mythtv/libs/libmythtv/mythplayer.cpp mythtv-0.27-20140504-ge2acf0d-new/mythtv/libs/libmythtv/mythplayer.cpp
--- mythtv-0.27-20140504-ge2acf0d-old/mythtv/libs/libmythtv/mythplayer.cpp	2014-05-05 11:58:04.885551685 +0200
+++ mythtv-0.27-20140504-ge2acf0d-new/mythtv/libs/libmythtv/mythplayer.cpp	2014-05-05 11:58:16.285486476 +0200
@@ -1019,11 +1019,14 @@
     {
         gCoreContext->SaveSetting(
             "DefaultChanid", player_ctx->playingInfo->GetChanID());
-        int cardid = player_ctx->recorder->GetRecorderNumber();
-        QString channum = player_ctx->playingInfo->GetChanNum();
-        QString inputname;
-        int cardinputid = CardUtil::GetCardInputID(cardid, channum, inputname);
-        CardUtil::SetStartChannel(cardinputid, channum);
+        if (player_ctx->recorder && player_ctx->recorder->IsValidRecorder())
+        {
+            int cardid = player_ctx->recorder->GetRecorderNumber();
+            QString channum = player_ctx->playingInfo->GetChanNum();
+            QString inputname;
+            int cardinputid = CardUtil::GetCardInputID(cardid, channum, inputname);
+            CardUtil::SetStartChannel(cardinputid, channum);
+        }
     }
 
     return IsErrored() ? -1 : 0;
