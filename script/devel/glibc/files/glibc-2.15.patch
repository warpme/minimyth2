diff -Naur glibc-2.15-old/configure glibc-2.15-new/configure
--- glibc-2.15-old/configure	2012-03-22 08:42:44.000000000 -0700
+++ glibc-2.15-new/configure	2012-03-22 08:43:48.000000000 -0700
@@ -4643,11 +4643,11 @@
     path_binutils=`(cd $path_binutils; pwd) | sed 's%/*$%/%'`
     CC="$CC -B$path_binutils"
 fi
-AS=`$CC -print-prog-name=as`
-LD=`$CC -print-prog-name=ld`
-AR=`$CC -print-prog-name=ar`
-
-OBJDUMP=`$CC -print-prog-name=objdump`
+AS=${AS}
+LD=${LD}
+AR=${AR}
+  
+OBJDUMP=${OBJDUMP}
 
 OBJCOPY=`$CC -print-prog-name=objcopy`
 
