diff -Naur mythtv-master-20190216-g938606f-old/mythplugins/mythmusic/mythmusic/mainvisual.cpp mythtv-master-20190216-g938606f-new/mythplugins/mythmusic/mythmusic/mainvisual.cpp
--- mythtv-master-20190216-g938606f-old/mythplugins/mythmusic/mythmusic/mainvisual.cpp	2019-02-17 16:59:02.633333336 +0100
+++ mythtv-master-20190216-g938606f-new/mythplugins/mythmusic/mythmusic/mainvisual.cpp	2019-02-17 16:59:03.846666669 +0100
@@ -18,6 +18,7 @@
 
 // mythtv
 #include <mythuivideo.h>
+#include <mythuihelper.h>
 
 // mythmusic
 #include "visualize.h"
@@ -60,7 +61,11 @@
     delete m_updateTimer;
 
     if (m_vis)
+    {
+        if (!m_vis->xscreensaverenable())
+            GetMythUI()->DoRestoreScreensaver();
         delete m_vis;
+    }
 
     while (!m_nodes.empty())
         delete m_nodes.takeLast();
@@ -79,6 +84,11 @@
     }
 }
 
+static bool inline IsVisFullscreen(QSize size)
+{
+    return size.width() >= 512 && size.height() >= 512;
+}
+
 void MainVisual::setVisual(const QString &name)
 {
     m_updateTimer->stop();
@@ -120,9 +130,12 @@
         if (pVisFactory->name() == visName)
         {
             m_vis = pVisFactory->create(this, pluginName);
-            m_vis->resize(m_visualizerVideo->GetArea().size());
+            const QSize size = m_visualizerVideo->GetArea().size();
+            m_vis->resize(size);
             m_fps = m_vis->getDesiredFPS();
             m_samples = m_vis->getDesiredSamples();
+            if (!m_vis->xscreensaverenable() && IsVisFullscreen(size))
+                GetMythUI()->DoDisableScreensaver();
 
             QMutexLocker locker(mutex());
             prepare();
@@ -249,7 +262,13 @@
     m_pixmap.fill(m_visualizerVideo->GetBackgroundColor());
 
     if (m_vis)
+    {
         m_vis->resize(size);
+        if (!m_vis->xscreensaverenable() && IsVisFullscreen(size))
+            GetMythUI()->DoDisableScreensaver();
+        else
+            GetMythUI()->DoRestoreScreensaver();
+    }
 }
 
 void MainVisual::customEvent(QEvent *event)
diff -Naur mythtv-master-20190216-g938606f-old/mythplugins/mythmusic/mythmusic/visualize.cpp mythtv-master-20190216-g938606f-new/mythplugins/mythmusic/mythmusic/visualize.cpp
--- mythtv-master-20190216-g938606f-old/mythplugins/mythmusic/mythmusic/visualize.cpp	2019-02-17 16:59:02.636666669 +0100
+++ mythtv-master-20190216-g938606f-new/mythplugins/mythmusic/mythmusic/visualize.cpp	2019-02-17 16:59:03.846666669 +0100
@@ -44,8 +44,6 @@
 VisualBase::VisualBase(bool screensaverenable)
     : m_xscreensaverenable(screensaverenable)
 {
-    if (!m_xscreensaverenable)
-        GetMythUI()->DoDisableScreensaver();
 }
 
 VisualBase::~VisualBase()
@@ -55,8 +53,6 @@
     //    that derived classes
     //    can destruct properly
     //
-    if (!m_xscreensaverenable)
-        GetMythUI()->DoRestoreScreensaver();
 }
 
 
diff -Naur mythtv-master-20190216-g938606f-old/mythplugins/mythmusic/mythmusic/visualize.h mythtv-master-20190216-g938606f-new/mythplugins/mythmusic/mythmusic/visualize.h
--- mythtv-master-20190216-g938606f-old/mythplugins/mythmusic/mythmusic/visualize.h	2019-02-17 16:59:02.633333336 +0100
+++ mythtv-master-20190216-g938606f-new/mythplugins/mythmusic/mythmusic/visualize.h	2019-02-17 16:59:03.846666669 +0100
@@ -93,6 +93,8 @@
     virtual unsigned long getDesiredSamples(void) { return SAMPLES_DEFAULT_SIZE; }
     void drawWarning(QPainter *p, const QColor &back, const QSize &size, QString warning, int fontsize = 28);
 
+    bool xscreensaverenable() const { return m_xscreensaverenable; }
+
   protected:
     int  m_fps                {20};
     bool m_xscreensaverenable {true};
