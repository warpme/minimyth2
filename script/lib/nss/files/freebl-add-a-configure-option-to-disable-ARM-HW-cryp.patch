diff --speed-large-files --no-dereference --minimal -Naur nss-3.122/nss/lib/freebl/Makefile nss-3.122/nss/lib/freebl/Makefile
--- nss-3.122/nss/lib/freebl/Makefile	2026-03-19 17:58:13.000000000 +0100
+++ nss-3.122/nss/lib/freebl/Makefile	2026-04-16 09:18:22.024045610 +0200
@@ -136,6 +136,8 @@
         endif
     endif
 endif
+ifdef NSS_USE_ARM_HW_CRYPTO
+    DEFINES += -DNSS_USE_ARM_HW_CRYPTO
 ifeq ($(CPU_ARCH),aarch64)
     ifdef CC_IS_CLANG
         DEFINES += -DUSE_HW_AES -DUSE_HW_SHA1 -DUSE_HW_SHA2 -DHAVE_PLATFORM_GHASH
@@ -178,6 +180,7 @@
         endif
     endif
 endif
+endif
 
 ifeq (,$(filter-out WINNT,$(OS_TARGET)))
 ifndef USE_64
