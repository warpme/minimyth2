diff --speed-large-files --no-dereference --minimal -Naur lirc-0.10.2/lib/lirc_client.c lirc-0.10.2/lib/lirc_client.c
--- lirc-0.10.2/lib/lirc_client.c	2022-09-25 12:03:38.000000000 +0200
+++ lirc-0.10.2/lib/lirc_client.c	2026-02-02 19:01:34.816215898 +0100
@@ -199,7 +199,7 @@
 
 	todo = strlen(ctx->packet);
 	data = ctx->packet;
-	logprintf(LIRC_DEBUG, "lirc_command_run: Sending: %s", data);
+	//logprintf(LIRC_DEBUG, "lirc_command_run: Sending: %s", data);
 	while (todo > 0) {
 		done = write(fd, (void*)data, todo);
 		if (done < 0) {
@@ -222,9 +222,9 @@
 		while (r == EAGAIN);
 		if (!string || strlen(string) == 0)
 			goto bad_packet;
-		logprintf(LIRC_DEBUG,
-			  "lirc_command_run, state: %d, input: \"%s\"\n",
-			  state, string ? string : "(Null)");
+		//logprintf(LIRC_DEBUG,
+		//	  "lirc_command_run, state: %d, input: \"%s\"\n",
+		//	  state, string ? string : "(Null)");
 		switch (state) {
 		case P_BEGIN:
 			if (strcasecmp(string, "BEGIN") != 0)
@@ -245,8 +245,8 @@
 			if (strcasecmp(string, "SUCCESS") == 0) {
 				status = 0;
 			} else if (strcasecmp(string, "END") == 0) {
-				logprintf(LIRC_NOTICE,
-					  "lirc_command_run: status:END");
+				//logprintf(LIRC_NOTICE,
+				//	  "lirc_command_run: status:END");
 				return 0;
 			} else if (strcasecmp(string, "ERROR") == 0) {
 				logprintf(LIRC_WARNING,
@@ -260,17 +260,17 @@
 			break;
 		case P_DATA:
 			if (strcasecmp(string, "END") == 0) {
-				logprintf(LIRC_NOTICE,
-					  "lirc_command_run: data:END, status:%d",
-					  status);
+				//logprintf(LIRC_NOTICE,
+				//	  "lirc_command_run: data:END, status:%d",
+				//	  status);
 				return status;
 			} else if (strcasecmp(string, "DATA") == 0) {
 				state = P_N;
 				break;
 			}
-			logprintf(LIRC_DEBUG,
-				  "data: bad packet: %s\n",
-				  string);
+			//logprintf(LIRC_DEBUG,
+			//	  "data: bad packet: %s\n",
+			//	  string);
 			goto bad_packet;
 		case P_N:
 			errno = 0;
@@ -304,9 +304,9 @@
 			break;
 		case P_END:
 			if (strcasecmp(string, "END") == 0) {
-				logprintf(LIRC_NOTICE,
-					  "lirc_command_run: status:END, status:%d",
-					  status);
+				//logprintf(LIRC_NOTICE,
+				//	  "lirc_command_run: status:END, status:%d",
+				//	  status);
 				return status;
 			}
 			goto bad_packet;
