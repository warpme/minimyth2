From eb1862c935cd7446dbedf124f7a02941c82015c2 Mon Sep 17 00:00:00 2001
From: Cristian Ciocaltea <cristian.ciocaltea@collabora.com>
Date: Sat, 14 Mar 2026 03:14:18 +0200
Subject: [PATCH 112/114] [WIP] dw-hdmi-qp: Code cleanup

Signed-off-by: Cristian Ciocaltea <cristian.ciocaltea@collabora.com>
---
 drivers/gpu/drm/bridge/synopsys/dw-hdmi-qp.c  | 485 +++++++++---------
 drivers/gpu/drm/display/drm_scdc_helper.c     | 140 +++++
 .../gpu/drm/rockchip/dw_hdmi_qp-rockchip.c    |  99 +---
 include/drm/bridge/dw_hdmi_qp.h               |   5 +-
 include/drm/display/drm_scdc.h                |   2 +
 include/drm/display/drm_scdc_helper.h         |   4 +
 6 files changed, 428 insertions(+), 307 deletions(-)

diff --git a/drivers/gpu/drm/bridge/synopsys/dw-hdmi-qp.c b/drivers/gpu/drm/bridge/synopsys/dw-hdmi-qp.c
index b533b5d800..df027e41ea 100644
--- a/drivers/gpu/drm/bridge/synopsys/dw-hdmi-qp.c
+++ b/drivers/gpu/drm/bridge/synopsys/dw-hdmi-qp.c
@@ -833,70 +833,28 @@ static struct i2c_adapter *dw_hdmi_qp_i2c_adapter(struct dw_hdmi_qp *hdmi)
 	return adap;
 }
 
-static int dw_hdmi_qp_frl_rate_to_scdc(unsigned long frl_rate)
-{
-	switch (frl_rate) {
-	case 48:
-		return SCDC_FRL_RATE_12GBPS_4LANE;
-	case 40:
-		return SCDC_FRL_RATE_10GBPS_4LANE;
-	case 32:
-		return SCDC_FRL_RATE_8GBPS_4LANE;
-	case 24:
-		return SCDC_FRL_RATE_6GBPS_4LANE;
-	case 18:
-		return SCDC_FRL_RATE_6GBPS_3LANE;
-	case 9:
-		return SCDC_FRL_RATE_3GBPS_3LANE;
-	}
-
-	return 0;
-}
-
-static int dw_hdmi_qp_frl_scdc_to_rate(u8 frl_scdc)
-{
-	switch (frl_scdc) {
-	case SCDC_FRL_RATE_12GBPS_4LANE:
-		return 48;
-	case SCDC_FRL_RATE_10GBPS_4LANE:
-		return 40;
-	case SCDC_FRL_RATE_8GBPS_4LANE:
-		return 32;
-	case SCDC_FRL_RATE_6GBPS_4LANE:
-		return 24;
-	case SCDC_FRL_RATE_6GBPS_3LANE:
-		return 18;
-	case SCDC_FRL_RATE_3GBPS_3LANE:
-		return 9;
-	}
-
-	return 0;
-}
-
 enum dw_hdmi_qp_flt_state {
 	LTS1,	/* Read EDID */
 	LTS2,	/* Prepare for FRL */
 	LTS3,	/* Training in progress */
 	LTS4,	/* Update FRL rate */
 	LTSP,	/* Training passed */
-	LTSL,	/* Exit FRL mode */
+	LTSL,	/* Legacy TMDS */
+	LTSU,	/* Undefined */
 };
 
 /* FRL training max ffe level: 0..3 */
+//TODO: support ffe > 0?!
 #define MAX_FFE_LEVEL	0
 
-static bool dw_hdmi_qp_is_disabled(struct dw_hdmi_qp *hdmi)
-{
-	return hdmi->phy.ops->read_hpd(hdmi, hdmi->phy.data) == connector_status_disconnected;
-}
-
 /* Check sink version and FLT no-timeout mode. */
 static int dw_hdmi_qp_flt_lts1(struct dw_hdmi_qp *hdmi)
 {
 	u8 val = 0;
+	int ret;
 
 	if (!hdmi->tmds_char_rate) {
-		dev_err(hdmi->dev, "lts1: hdmi dclk is disabled\n");
+		dev_dbg(hdmi->dev, "lts1: hdmi disabled\n");
 		return LTSL;
 	}
 
@@ -906,16 +864,31 @@ static int dw_hdmi_qp_flt_lts1(struct dw_hdmi_qp *hdmi)
 	/* Reset AVP data path */
 	dw_hdmi_qp_write(hdmi, AVP_DATAPATH_SWINIT_P, GLOBAL_SWRESET_REQUEST);
 
-	drm_scdc_readb(hdmi->bridge.ddc, SCDC_SINK_VERSION, &val);
+	ret = drm_scdc_readb(hdmi->bridge.ddc, SCDC_SINK_VERSION, &val);
+	if (ret) {
+		dev_err(hdmi->dev, "lts1: SCDC read failed\n");
+		return LTSL;
+	}
+
 	if (!val) {
 		dev_err(hdmi->dev, "lts1: SCDC sink version is zero\n");
 		return LTSL;
 	}
 
-	drm_scdc_writeb(hdmi->bridge.ddc, SCDC_SOURCE_VERSION, 1);
+	ret = drm_scdc_writeb(hdmi->bridge.ddc, SCDC_SOURCE_VERSION, 1);
+	if (ret) {
+		dev_err(hdmi->dev, "lts1: SCDC write failed\n");
+		return LTSL;
+	}
+
+	ret = drm_scdc_readb(hdmi->bridge.ddc, SCDC_SOURCE_TEST_CONFIG, &val);
+	if (ret) {
+		dev_err(hdmi->dev, "lts1: SCDC read failed\n");
+		return LTSL;
+	}
 
-	drm_scdc_readb(hdmi->bridge.ddc, SCDC_SOURCE_TEST_CONFIG, &val);
 	hdmi->flt_no_timeout = !!(val & SCDC_FLT_NO_TIMEOUT);
+	dev_dbg(hdmi->dev, "lts1: flt_no_timeout=%d\n", hdmi->flt_no_timeout);
 
 	return LTS2;
 }
@@ -924,171 +897,208 @@ static int dw_hdmi_qp_flt_lts1(struct dw_hdmi_qp *hdmi)
  * Check if sink is ready to training.
  * Set source output frl rate & max ffe level.
  */
-static int dw_hdmi_qp_flt_lts2(struct dw_hdmi_qp *hdmi, unsigned long rate)
+static int dw_hdmi_qp_flt_lts2(struct dw_hdmi_qp *hdmi)
 {
-	u8 scdc_rate = dw_hdmi_qp_frl_rate_to_scdc(rate);
+	const struct dw_hdmi_qp_link_config *link_cfg;
 	u8 val = 0;
-	int i;
+	int ret, i;
 
 	for (i = 0; i < 20; i++) {
-		drm_scdc_readb(hdmi->bridge.ddc, SCDC_STATUS_FLAGS_0, &val);
-		if (val & SCDC_FLT_READY)
-			break;
-		msleep(20);
-	}
+		ret = drm_scdc_readb(hdmi->bridge.ddc, SCDC_STATUS_FLAGS_0, &val);
+		if (ret) {
+			dev_err(hdmi->dev, "lts2: SCDC read failed\n");
+			return LTSL;
+		}
 
-	if (i == 20) {
-		dev_err(hdmi->dev, "lts2: sink flt isn't ready (SCDC_STATUS_FLAGS_0=0x%x)\n", val);
-		return LTSL;
-	}
+		if (val & SCDC_FLT_READY) {
+			link_cfg = hdmi->phy.ops->get_link_cfg(hdmi, hdmi->phy.data);
 
-	val = MAX_FFE_LEVEL << 4 | scdc_rate;
-	drm_scdc_writeb(hdmi->bridge.ddc, SCDC_CONFIG_1, val);
-	drm_scdc_writeb(hdmi->bridge.ddc, SCDC_CONFIG_0, 0);
+			dev_dbg(hdmi->dev, "lts2: rate=%ux%u maxffe=%d\n",
+				link_cfg->frl_rate_per_lane, link_cfg->frl_lanes, MAX_FFE_LEVEL);
 
-	return LTS3;
-}
+			ret = drm_scdc_set_frl(hdmi->curr_conn, link_cfg->frl_rate_per_lane,
+					       link_cfg->frl_lanes, MAX_FFE_LEVEL);
+			if (ret)
+				ret = drm_scdc_writeb(hdmi->bridge.ddc, SCDC_CONFIG_0, 0);
+			else
+				ret = -EIO;
 
-static void dw_hdmi_qp_set_ltp(struct dw_hdmi_qp *hdmi, u32 value, bool flt_no_timeout)
-{
-	/* Support hfr1-10, send old ltp when all lane is 3 */
-	if (!flt_no_timeout && value == 0x3333f)
-		value = dw_hdmi_qp_read(hdmi, FLT_CONFIG1);
+			if (ret) {
+				dev_err(hdmi->dev, "lts2: SCDC write failed\n");
+				return LTSL;
+			}
+
+			return LTS3;
+		}
+
+		msleep(20);
+	}
 
-	dw_hdmi_qp_write(hdmi, value, FLT_CONFIG1);
+	dev_err(hdmi->dev, "lts2: sink flt not ready\n");
+	return LTSL;
 }
 
 /*
  * Conduct link training for the specified frl rate.
  * Send sink request ltp or change ffe level.
  */
-static int dw_hdmi_qp_flt_lts3(struct dw_hdmi_qp *hdmi, unsigned long rate)
+static int dw_hdmi_qp_flt_lts3(struct dw_hdmi_qp *hdmi)
 {
-	u8 val;
-	int i = 0, ret = 0;
-	u8 src_test_cfg = 0;
-	u32 value;
-	u8 ffe_lv = 0;
+	u8 val, ffe_lv = 0;
+	int i, ret;
 
-	/* Set max 2s timeout */
+	/* Set 2s timeout */
 	i = 4000;
 	while (i-- > 0 || hdmi->flt_no_timeout) {
-		if (dw_hdmi_qp_is_disabled(hdmi)) {
-			dev_dbg(hdmi->dev, "lts3: hdmi dclk is disabled, stop flt\n");
-			break;
-		}
-
-		/* source should poll update flag every 2ms or less */
+		/* Poll FLT_update flag every 2ms or less */
 		usleep_range(400, 500);
 
-		drm_scdc_readb(hdmi->bridge.ddc, SCDC_UPDATE_0, &val);
+		if (!hdmi->tmds_char_rate) {
+			dev_dbg(hdmi->dev, "lts3: hdmi disabled\n");
+			return LTSL;
+		}
+
+		ret = drm_scdc_readb(hdmi->bridge.ddc, SCDC_UPDATE_0, &val);
+		if (ret) {
+			dev_err(hdmi->dev, "lts3: SCDC read failed\n");
+			return LTSL;
+		}
 
 		if (val & SCDC_SOURCE_TEST_UPDATE) {
-			/* Quit test mode */
-			drm_scdc_readb(hdmi->bridge.ddc, SCDC_SOURCE_TEST_CONFIG, &src_test_cfg);
+			u8 test_cfg;
+			ret = drm_scdc_readb(hdmi->bridge.ddc, SCDC_SOURCE_TEST_CONFIG,
+					     &test_cfg);
+			if (ret) {
+				dev_err(hdmi->dev, "lts3: SCDC read failed\n");
+				return LTSL;
+			}
 
-			if (hdmi->flt_no_timeout && !(src_test_cfg & SCDC_FLT_NO_TIMEOUT)) {
+			if (hdmi->flt_no_timeout && !(test_cfg & SCDC_FLT_NO_TIMEOUT)) {
 				dev_dbg(hdmi->dev, "lts3: exit test mode\n");
 				hdmi->flt_no_timeout = false;
-			} else if (!hdmi->flt_no_timeout && (src_test_cfg & SCDC_FLT_NO_TIMEOUT)) {
+			} else if (!hdmi->flt_no_timeout && (test_cfg & SCDC_FLT_NO_TIMEOUT)) {
 				dev_dbg(hdmi->dev, "lts3: enter test mode\n");
 				hdmi->flt_no_timeout = true;
 			}
 		}
 
 		if (!(val & SCDC_CONFIG_0)) {
-			/* Clear SOURCE_TEST_UPDATE flag */
-			if (val & SCDC_SOURCE_TEST_UPDATE)
-				drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, val);
+			if (val & SCDC_SOURCE_TEST_UPDATE) {
+				/* Clear SOURCE_TEST_UPDATE flag */
+				ret = drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, val);
+				if (ret) {
+					dev_err(hdmi->dev, "lts3: SCDC write failed\n");
+					return LTSL;
+				}
+			}
+
 			continue;
 		}
 
 		if (val & SCDC_FLT_UPDATE) {
-			u8 reg_val, ln0, ln1, ln2, ln3;
+			u8 status, ln0, ln1, ln2, ln3;
+			u32 flt_cfg;
+			int lts_next = 0;
+
+			ret = drm_scdc_readb(hdmi->bridge.ddc, SCDC_STATUS_FLAGS_1, &status);
+			if (ret) {
+				dev_err(hdmi->dev, "lts3: SCDC read failed\n");
+				return LTSL;
+			}
 
-			drm_scdc_readb(hdmi->bridge.ddc, SCDC_STATUS_FLAGS_1, &reg_val);
-			ln0 = reg_val & 0xf;
-			ln1 = (reg_val >> 4) & 0xf;
+			ln0 = status & 0xf;
+			ln1 = (status >> 4) & 0xf;
 
-			drm_scdc_readb(hdmi->bridge.ddc, SCDC_STATUS_FLAGS_2, &reg_val);
-			ln2 = reg_val & 0xf;
-			ln3 = (reg_val >> 4) & 0xf;
+			ret = drm_scdc_readb(hdmi->bridge.ddc, SCDC_STATUS_FLAGS_2, &status);
+			if (ret) {
+				dev_err(hdmi->dev, "lts3: SCDC read failed\n");
+				return LTSL;
+			}
+
+			ln2 = status & 0xf;
+			ln3 = (status >> 4) & 0xf;
 
 			dev_dbg(hdmi->dev, "lts3: ln0=0x%x ln1=0x%x ln0x%x ln3=0x%x\n",
 				ln0, ln1, ln2, ln3);
 
 			if (!ln0 && !ln1 && !ln2 && !ln3) {
-				dev_dbg(hdmi->dev, "lts3: flt completed, go to ltsp\n");
-				if (hdmi->tmds_char_rate) {
-					dw_hdmi_qp_write(hdmi, 0, FLT_CONFIG1);
-					ret = LTSP;
-				} else {
-					dev_err(hdmi->dev, "lts3: hdmi dclk is disabled, goto ltsp\n");
-					ret = LTSL;
-				}
+				dw_hdmi_qp_write(hdmi, 0, FLT_CONFIG1);
+				lts_next = LTSP;
 			} else if ((ln0 == 0xf) || (ln1 == 0xf) || (ln2 == 0xf) || (ln3 == 0xf)) {
-				dev_dbg(hdmi->dev, "lts3: goto lts4\n");
-				ret = LTS4;
+				dev_dbg(hdmi->dev, "lts3: rate change request\n");
+				lts_next = LTS4;
 			} else if ((ln0 == 0xe) || (ln1 == 0xe) || (ln2 == 0xe) || (ln3 == 0xe)) {
 				if (ffe_lv < 3) {
 					++ffe_lv;
 					dev_dbg(hdmi->dev, "lts3: set ffe=%d\n", ffe_lv);
 					//TODO: set_ffe()
-					/* hdmi->phy.ops->set_ffe(hdmi, hdmi->phy.data, ++ffe_lv); */
+					/* hdmi->phy.ops->set_ffe(hdmi, hdmi->phy.data, ffe_lv); */
 				} else {
 					dev_err(hdmi->dev, "lts3: ffe level out of range\n");
-					ret = LTSL;
+					lts_next = LTSL;
 				}
 			} else {
-				if (hdmi->tmds_char_rate) {
-					value = (ln3 << 16) | (ln2 << 12) | (ln1 << 8) |
-						(ln0 << 4) | 0xf;
+				flt_cfg = (ln3 << 16) | (ln2 << 12) | (ln1 << 8) |
+					  (ln0 << 4) | 0xf;
 
-					dw_hdmi_qp_set_ltp(hdmi, value, hdmi->flt_no_timeout);
-				} else {
-					dev_err(hdmi->dev, "lts3: hdmi dclk is disabled, set ltp failed\n");
-					ret = LTSL;
-				}
+				/* Support hfr1-10, send old ltp when all lanes == 0x3 */
+				if (!hdmi->flt_no_timeout && flt_cfg == 0x3333f)
+					flt_cfg = dw_hdmi_qp_read(hdmi, FLT_CONFIG1);
+
+				dw_hdmi_qp_write(hdmi, flt_cfg, FLT_CONFIG1);
 			}
 
 			/* Clear FLT_UPDATE flag */
-			drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, val);
-		}
-
-		if (ret)
-			break;
-	}
+			ret = drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, val);
+			if (ret) {
+				dev_err(hdmi->dev, "lts3: SCDC write failed\n");
+				return LTSL;
+			}
 
-	if (!ret) {
-		ret = LTSL;
-		dev_err(hdmi->dev, "lts3 time out, goto ltsl\n");
+			if (lts_next)
+				return lts_next;
+		}
 	}
 
-	return ret;
+	dev_err(hdmi->dev, "lts3: timed out\n");
+	return LTSL;
 }
 
 /* Sink requests frl rate change, start training for a new rate. */
-static int dw_hdmi_qp_flt_lts4(struct dw_hdmi_qp *hdmi, unsigned long *rate)
+static int dw_hdmi_qp_flt_lts4(struct dw_hdmi_qp *hdmi)
 {
-	u8 scdc_rate = dw_hdmi_qp_frl_rate_to_scdc(*rate);
-	void *data = hdmi->phy.data;
-	unsigned long actual_rate;
+	const struct dw_hdmi_qp_link_config *link_cfg;
+	u8 try_rate_per_lane, try_lanes;
+	int ret;
+
+	link_cfg = hdmi->phy.ops->get_link_cfg(hdmi, hdmi->phy.data);
+
+	/* Choose a reduced bandwith FRL rate */
+	ret = drm_scdc_calc_lower_frl(link_cfg->frl_rate_per_lane, link_cfg->frl_lanes,
+				      &try_rate_per_lane, &try_lanes);
+	if (ret == false) {
+		dev_err(hdmi->dev, "lts4: failed to compute lower frl\n");
+		return LTSL;
+	}
 
 	/* Don't use frl rates below 32G */
-	if (scdc_rate == SCDC_FRL_RATE_8GBPS_4LANE) {
-		dev_err(hdmi->dev, "lts4: 32G limit reached, goto ltsl\n");
+	//TODO: what is the actual limit?
+	if (try_rate_per_lane * try_lanes < 32) {
+		dev_err(hdmi->dev, "lts4: 32Gbps limit reached\n");
 		return LTSL;
 	}
 
+	dev_dbg(hdmi->dev, "lts4: switching to %ux%u\n", try_rate_per_lane, try_lanes);
+
 	/* Disable phy */
 	hdmi->phy.ops->disable(hdmi, hdmi->phy.data);
 
-	/* Set lower frl rate */
-	scdc_rate--;
-	actual_rate = dw_hdmi_qp_frl_scdc_to_rate(scdc_rate);
+	//TODO: op shall be mandatory
 	if (hdmi->phy.ops->force_link_rate) {
-		//TODO: handle error
-		hdmi->phy.ops->force_link_rate(hdmi, data, actual_rate);
+		ret = hdmi->phy.ops->force_link_rate(hdmi, hdmi->phy.data,
+						     try_rate_per_lane, try_lanes);
+		dev_err(hdmi->dev, "lts4: failed to set phy link rate\n");
+		return LTSL;
 	}
 
 	/* Enable phy */
@@ -1097,11 +1107,18 @@ static int dw_hdmi_qp_flt_lts4(struct dw_hdmi_qp *hdmi, unsigned long *rate)
 	hdmi->phy.ops->init(hdmi, hdmi->phy.data);
 
 	/* Set new rate */
-	*rate = actual_rate;
-	drm_scdc_writeb(hdmi->bridge.ddc, SCDC_CONFIG_1, (MAX_FFE_LEVEL << 4 | scdc_rate));
-	drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, SCDC_FLT_UPDATE);
+	ret = drm_scdc_set_frl(hdmi->curr_conn, try_rate_per_lane, try_lanes,
+			       MAX_FFE_LEVEL);
+	if (ret)
+		ret = drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, SCDC_FLT_UPDATE);
+	else
+		ret = -EIO;
+
+	if (ret) {
+		dev_err(hdmi->dev, "lts4: SCDC write failed\n");
+		return LTSL;
+	}
 
-	dev_dbg(hdmi->dev, "lts4: goto lts3\n");
 	return LTS3;
 }
 
@@ -1111,84 +1128,106 @@ static int dw_hdmi_qp_flt_lts4(struct dw_hdmi_qp *hdmi, unsigned long *rate)
  */
 static int dw_hdmi_qp_flt_ltsp(struct dw_hdmi_qp *hdmi)
 {
-	u8 val = 0;
 	int i = 4000;
+	u8 val;
+	int ret;
 
-	/* Wait for frl start */
+	/* Wait for FRL start by polling FLT_update flag every 2ms or less */
 	while (i--) {
-		if (dw_hdmi_qp_is_disabled(hdmi)) {
-			dev_dbg(hdmi->dev, "hdmi dclk is disabled, quit ltsp\n");
+		usleep_range(400, 500);
+
+		if (!hdmi->tmds_char_rate) {
+			dev_dbg(hdmi->dev, "ltsp: hdmi disabled\n");
 			return LTSL;
 		}
 
-		/* Source should poll update flag every 2ms or less */
-		usleep_range(400, 500);
-
-		drm_scdc_readb(hdmi->bridge.ddc, SCDC_UPDATE_0, &val);
+		ret = drm_scdc_readb(hdmi->bridge.ddc, SCDC_UPDATE_0, &val);
+		if (ret) {
+			dev_err(hdmi->dev, "ltsp: SCDC read failed\n");
+			return LTSL;
+		}
 
 		if (!(val & SCDC_CONFIG_0))
 			continue;
 
-		if (hdmi->tmds_char_rate) {
-			if (val & SCDC_FRL_START) {
-				dw_hdmi_qp_mod(hdmi, 0, AVP_DATAPATH_VIDEO_SWDISABLE, GLOBAL_SWDISABLE);
-				drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, SCDC_FRL_START);
-				dw_hdmi_qp_write(hdmi, 2, PKTSCHED_PKT_CONTROL0);
-				dw_hdmi_qp_mod(hdmi, PKTSCHED_GCP_TX_EN, PKTSCHED_GCP_TX_EN,
-					       PKTSCHED_PKT_EN);
-				dev_dbg(hdmi->dev, "flt success\n");
-				break;
-			} else if (val & SCDC_FLT_UPDATE) {
-				dw_hdmi_qp_mod(hdmi, AVP_DATAPATH_VIDEO_SWDISABLE,
-					       AVP_DATAPATH_VIDEO_SWDISABLE, GLOBAL_SWDISABLE);
-				drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, SCDC_FLT_UPDATE);
-				return LTS3;
+		if (val & SCDC_FRL_START) {
+			dw_hdmi_qp_mod(hdmi, 0, AVP_DATAPATH_VIDEO_SWDISABLE, GLOBAL_SWDISABLE);
+
+			ret = drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, SCDC_FRL_START);
+			if (ret) {
+				dev_err(hdmi->dev, "ltsp: SCDC write failed\n");
+				return LTSL;
 			}
-		} else {
-			dev_err(hdmi->dev, "frl start failed (hdmi dclk is disabled)\n");
-			return LTSL;
+
+			dw_hdmi_qp_write(hdmi, 2, PKTSCHED_PKT_CONTROL0);
+			dw_hdmi_qp_mod(hdmi, PKTSCHED_GCP_TX_EN, PKTSCHED_GCP_TX_EN,
+				       PKTSCHED_PKT_EN);
+
+			dev_dbg(hdmi->dev, "ltsp: flt success\n");
+			break;
+		}
+
+		if (val & SCDC_FLT_UPDATE) {
+			dw_hdmi_qp_mod(hdmi, AVP_DATAPATH_VIDEO_SWDISABLE,
+				       AVP_DATAPATH_VIDEO_SWDISABLE, GLOBAL_SWDISABLE);
+
+			ret = drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, SCDC_FLT_UPDATE);
+			if (ret) {
+				dev_err(hdmi->dev, "ltsp: SCDC write failed\n");
+				return LTSL;
+			}
+
+			return LTS3;
 		}
 	}
 
 	if (i < 0) {
-		dev_err(hdmi->dev, "frl start timed out (SCDC_UPDATE_0=0x%x)\n", val);
+		dev_err(hdmi->dev, "ltsp: timed out\n");
 		return LTSL;
 	}
 
-	/*
-	 * After flt success, source shall poll update_flag at least once
-	 * every 250ms.
-	 */
+	/* Ensure FLT_update flag is polled at least once every 250ms. */
 	i = 5;
-	while (1) {
+
+	while (true) {
+		msleep(20);
+
 		if (!hdmi->tmds_char_rate) {
-			dev_dbg(hdmi->dev, "hdmi disconnected, stop poll flt_update\n");
+			dev_dbg(hdmi->dev, "ltsp: hdmi disabled\n");
 			return LTSL;
 		}
 
-		if (!i) {
-			i = drm_scdc_readb(hdmi->bridge.ddc, SCDC_UPDATE_0, &val);
-			if (i) {
-				dev_dbg(hdmi->dev, "DDC communication failed, stop poll flt_update\n");
-				return LTSL;
-			}
+		if (i) {
+			i--;
+			continue;
+		}
+
+		ret = drm_scdc_readb(hdmi->bridge.ddc, SCDC_UPDATE_0, &val);
+		if (ret) {
+			dev_err(hdmi->dev, "ltsp: SCDC read failed\n");
+			return LTSL;
+		}
+
+		if (val & SCDC_FLT_UPDATE) {
+			dw_hdmi_qp_write(hdmi, 1, PKTSCHED_PKT_CONTROL0);
+			dw_hdmi_qp_mod(hdmi, PKTSCHED_GCP_TX_EN, PKTSCHED_GCP_TX_EN,
+				       PKTSCHED_PKT_EN);
 
-			if (val & SCDC_FLT_UPDATE) {
-				dw_hdmi_qp_write(hdmi, 1, PKTSCHED_PKT_CONTROL0);
-				dw_hdmi_qp_mod(hdmi, PKTSCHED_GCP_TX_EN, PKTSCHED_GCP_TX_EN,
-					       PKTSCHED_PKT_EN);
-				msleep(50);
-				dw_hdmi_qp_mod(hdmi, AVP_DATAPATH_VIDEO_SWDISABLE,
-					       AVP_DATAPATH_VIDEO_SWDISABLE, GLOBAL_SWDISABLE);
-				drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, SCDC_FLT_UPDATE);
-				return LTS2;
+			msleep(50);
+			dw_hdmi_qp_mod(hdmi, AVP_DATAPATH_VIDEO_SWDISABLE,
+				       AVP_DATAPATH_VIDEO_SWDISABLE, GLOBAL_SWDISABLE);
+
+			ret = drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0,
+					      SCDC_FLT_UPDATE);
+			if (ret) {
+				dev_err(hdmi->dev, "ltsp: SCDC write failed\n");
+				return LTSL;
 			}
 
-			i = 5;
+			return LTS2;
 		}
 
-		msleep(20);
-		i--;
+		i = 5;
 	}
 
 	return LTSL;
@@ -1197,46 +1236,43 @@ static int dw_hdmi_qp_flt_ltsp(struct dw_hdmi_qp *hdmi)
 /* Exit frl mode, i.e. for training failures or hdmi disabled. */
 static int dw_hdmi_qp_flt_ltsl(struct dw_hdmi_qp *hdmi)
 {
+	enum drm_connector_status status;
 	/* if (hdmi->frl_switch) */
 	/* 	return -EINVAL; */
 
-	if (!dw_hdmi_qp_is_disabled(hdmi)) {
+	dev_dbg(hdmi->dev, "ltsl: exit flr\n");
+
+	status = hdmi->phy.ops->read_hpd(hdmi, hdmi->phy.data);
+
+	if (status != connector_status_disconnected) {
 		drm_scdc_writeb(hdmi->bridge.ddc, SCDC_CONFIG_1, 0);
 		drm_scdc_writeb(hdmi->bridge.ddc, SCDC_UPDATE_0, SCDC_FLT_UPDATE);
 	}
 
-	return -EINVAL;
+	return LTSU;
 }
 
 static void dw_hdmi_qp_flt_work(struct work_struct *work)
 {
 	struct dw_hdmi_qp *hdmi = container_of(work, struct dw_hdmi_qp, flt_work);
-	const struct dw_hdmi_qp_link_config *link_cfg;
-	unsigned long frl_rate;
-	int state = LTS1;
-
-	link_cfg = hdmi->phy.ops->get_link_cfg(hdmi, hdmi->phy.data);
-	frl_rate = link_cfg->frl_lanes * link_cfg->frl_rate_per_lane;
-
-	dev_dbg(hdmi->dev, "%s tmds_rate=%llu frl_rate=%lu\n", __func__,
-		hdmi->tmds_char_rate, frl_rate);
+	enum dw_hdmi_qp_flt_state state = LTS1;
 
 	/* if (hdmi->frl_switch) */
 	/* 	return; */
 
-	while (1) {
+	while (true) {
 		switch (state) {
 		case LTS1:
 			state = dw_hdmi_qp_flt_lts1(hdmi);
 			break;
 		case LTS2:
-			state = dw_hdmi_qp_flt_lts2(hdmi, frl_rate);
+			state = dw_hdmi_qp_flt_lts2(hdmi);
 			break;
 		case LTS3:
-			state = dw_hdmi_qp_flt_lts3(hdmi, frl_rate);
+			state = dw_hdmi_qp_flt_lts3(hdmi);
 			break;
 		case LTS4:
-			state = dw_hdmi_qp_flt_lts4(hdmi, &frl_rate);
+			state = dw_hdmi_qp_flt_lts4(hdmi);
 			break;
 		case LTSP:
 			state = dw_hdmi_qp_flt_ltsp(hdmi);
@@ -1244,13 +1280,11 @@ static void dw_hdmi_qp_flt_work(struct work_struct *work)
 		case LTSL:
 			state = dw_hdmi_qp_flt_ltsl(hdmi);
 			break;
+		case LTSU:
+			return;
 		default:
-			dev_err(hdmi->dev, "flt failed: state=%d\n", state);
-		}
-
-		if (state <= 0) {
-			dev_dbg(hdmi->dev, "%s stopped\n", __func__);
-			break;
+			dev_err(hdmi->dev, "unexpected flt state: %d\n", state);
+			return;
 		}
 	}
 }
@@ -1436,16 +1470,8 @@ static void dw_hdmi_qp_bridge_atomic_enable(struct drm_bridge *bridge,
 			if (conn_state->hdmi.tmds_char_rate > HDMI14_MAX_TMDSCLK)
 				dw_hdmi_qp_enable_scramb(hdmi);
 		} else {
-			dev_dbg(hdmi->dev, "%s frl_rate_forced=%lu frl_rate_per_lane=%u frl_lanes=%u\n",
-				__func__, link_cfg->frl_rate_forced,
-				link_cfg->frl_rate_per_lane, link_cfg->frl_lanes);
-
-			if (link_cfg->frl_lanes == 4)
-				dw_hdmi_qp_mod(hdmi, OPMODE_FRL_4LANES,
-					       OPMODE_FRL_4LANES, LINK_CONFIG0);
-			else
-				dw_hdmi_qp_mod(hdmi, 0, OPMODE_FRL_4LANES, LINK_CONFIG0);
-
+			dw_hdmi_qp_mod(hdmi, link_cfg->frl_lanes == 4 ? OPMODE_FRL_4LANES : 0,
+				       OPMODE_FRL_4LANES, LINK_CONFIG0);
 			dw_hdmi_qp_mod(hdmi, 1, OPMODE_FRL, LINK_CONFIG0);
 		}
 	} else {
@@ -1458,16 +1484,15 @@ static void dw_hdmi_qp_bridge_atomic_enable(struct drm_bridge *bridge,
 
 	drm_atomic_helper_connector_hdmi_update_infoframes(hdmi->curr_conn, state);
 
-	//TODO: handle error
 	hdmi->phy.ops->init(hdmi, hdmi->phy.data);
 
 	if (conn_state->hdmi.tmds_char_rate > HDMI20_MAX_TMDSRATE) {
-		/* wait phy output stable then start flt */
+		/* Ensure phy output is stable before starting FLT */
 		msleep(50);
 		schedule_work(&hdmi->flt_work);
-		//TODO: only the flt poll should be handled in workqueue
 	}
 
+	//TODO: frl_switch needed?!
 	/* hdmi->frl_switch = false; */
 }
 
diff --git a/drivers/gpu/drm/display/drm_scdc_helper.c b/drivers/gpu/drm/display/drm_scdc_helper.c
index df878aad4a..f6febd5e6f 100644
--- a/drivers/gpu/drm/display/drm_scdc_helper.c
+++ b/drivers/gpu/drm/display/drm_scdc_helper.c
@@ -21,6 +21,7 @@
  * DEALINGS IN THE SOFTWARE.
  */
 
+#include <linux/bitfield.h>
 #include <linux/export.h>
 #include <linux/i2c.h>
 #include <linux/slab.h>
@@ -276,3 +277,142 @@ bool drm_scdc_set_high_tmds_clock_ratio(struct drm_connector *connector,
 	return true;
 }
 EXPORT_SYMBOL(drm_scdc_set_high_tmds_clock_ratio);
+
+static int drm_scdc_frl_config_to_rate(u8 config, u8 *rate_per_lane, u8 *lanes)
+{
+	switch (config) {
+	case SCDC_FRL_RATE_12GBPS_4LANE:
+		*rate_per_lane = 12;
+		*lanes = 4;
+		return 0;
+	case SCDC_FRL_RATE_10GBPS_4LANE:
+		*rate_per_lane = 10;
+		*lanes = 4;
+		return 0;
+	case SCDC_FRL_RATE_8GBPS_4LANE:
+		*rate_per_lane = 8;
+		*lanes = 4;
+		return 0;
+	case SCDC_FRL_RATE_6GBPS_4LANE:
+		*rate_per_lane = 6;
+		*lanes = 4;
+		return 0;
+	case SCDC_FRL_RATE_6GBPS_3LANE:
+		*rate_per_lane = 6;
+		*lanes = 3;
+		return 0;
+	case SCDC_FRL_RATE_3GBPS_3LANE:
+		*rate_per_lane = 3;
+		*lanes = 3;
+		return 0;
+	case SCDC_FRL_RATE_DISABLE:
+		*rate_per_lane = 0;
+		*lanes = 0;
+		return 0;
+	default:
+		return -EINVAL;
+	}
+}
+
+static int drm_scdc_frl_rate_to_config(u8 rate_per_lane, u8 lanes)
+{
+	if (lanes != 3 && lanes != 4)
+		return -EINVAL;
+
+	switch (rate_per_lane * lanes) {
+	case 48:
+		return SCDC_FRL_RATE_12GBPS_4LANE;
+	case 40:
+		return SCDC_FRL_RATE_10GBPS_4LANE;
+	case 32:
+		return SCDC_FRL_RATE_8GBPS_4LANE;
+	case 24:
+		return SCDC_FRL_RATE_6GBPS_4LANE;
+	case 18:
+		return SCDC_FRL_RATE_6GBPS_3LANE;
+	case 9:
+		return SCDC_FRL_RATE_3GBPS_3LANE;
+	case 0:
+		return SCDC_FRL_RATE_DISABLE;
+	default:
+		return -EINVAL;
+	}
+}
+
+/**
+ * drm_scdc_set_frl - set FRL rate and FFE
+ * @connector: connector
+ * @rate_per_lane: FRL rate for a single lane (Gbps)
+ * @lanes: FRL lane count (3 or 4)
+ * @max_ffe_level: max TxFFE level for indicated FRL Rate (0..3)
+ *
+ * Writes the FRL config register over SCDC channel, and:
+ *	sets FRL_Rate according to rate_per_lane * lanes
+ *	sets FFE_levels according to max_ffe_level
+ *
+ * Returns:
+ * True if write is successful, false otherwise.
+ */
+bool drm_scdc_set_frl(struct drm_connector *connector,
+		      u8 rate_per_lane, u8 lanes, u8 max_ffe_level)
+{
+	u8 config;
+	int ret;
+
+	ret = drm_scdc_frl_rate_to_config(rate_per_lane, lanes);
+	if (ret < 0 || max_ffe_level > 3) {
+		drm_dbg_kms(connector->dev,
+			    "[CONNECTOR:%d:%s] Invalid FRL config: rate=%ux%u ffe=%u\n",
+			    connector->base.id, connector->name, rate_per_lane,
+			    lanes, max_ffe_level);
+		return false;
+	}
+
+	config = FIELD_PREP(SCDC_FRL_RATE_MASK, ret) |
+		 FIELD_PREP(SCDC_FFE_LEVELS_MASK, max_ffe_level);
+
+	ret = drm_scdc_writeb(connector->ddc, SCDC_CONFIG_1, config);
+	if (ret < 0) {
+		drm_dbg_kms(connector->dev,
+			    "[CONNECTOR:%d:%s] Failed to set FRL: %d\n",
+			    connector->base.id, connector->name, ret);
+		return false;
+	}
+
+	return true;
+}
+EXPORT_SYMBOL(drm_scdc_set_frl);
+
+/**
+ * drm_scdc_calc_lower_frl - compute a reduced bandwith FRL rate
+ * @in_rate_per_lane: input FRL rate for a single lane (Gbps)
+ * @in_lanes: input FRL lane count (3 or 4)
+ * @out_rate_per_lane: output FRL rate for a single lane (Gbps)
+ * @out_lanes: output FRL lane count (3 or 4)
+ *
+ * Determinates the FLR rate configuration with the highest bandwidth that is
+ * still lower than the bandwith corresponding to the given input configuration.
+ * The resulting configuration is stored in out_rate_per_lane and out_lanes.
+ *
+ * Returns:
+ * True if computation was successful, false otherwise.
+ */
+bool drm_scdc_calc_lower_frl(u8 in_rate_per_lane, u8 in_lanes,
+			     u8 *out_rate_per_lane, u8 *out_lanes)
+{
+	int ret;
+
+	ret = drm_scdc_frl_rate_to_config(in_rate_per_lane, in_lanes);
+	if (ret < 0)
+		return false;
+
+	--ret;
+
+	if (ret <= SCDC_FRL_RATE_DISABLE)
+		return false;
+
+	ret = drm_scdc_frl_config_to_rate(ret, out_rate_per_lane, out_lanes);
+
+	return ret == 0;
+}
+EXPORT_SYMBOL(drm_scdc_calc_lower_frl);
diff --git a/drivers/gpu/drm/rockchip/dw_hdmi_qp-rockchip.c b/drivers/gpu/drm/rockchip/dw_hdmi_qp-rockchip.c
index b3fffb67ca..d7d8b062e5 100644
--- a/drivers/gpu/drm/rockchip/dw_hdmi_qp-rockchip.c
+++ b/drivers/gpu/drm/rockchip/dw_hdmi_qp-rockchip.c
@@ -170,54 +170,12 @@ static void dw_hdmi_qp_rockchip_encoder_enable(struct drm_encoder *encoder)
 	if (hdmi->ctrl_ops->enc_init)
 		hdmi->ctrl_ops->enc_init(hdmi, rks);
 
-	dev_dbg(hdmi->dev, "%s port=%d tmds=%llu bpc=%u frl_enabled=%d frl_rate_forced=%lu"
+	dev_dbg(hdmi->dev, "%s port=%d tmds=%llu bpc=%u frl_enabled=%d frl_rate_forced=%u"
 		" frl_rate_per_lane=%u frl_lanes=%u\n", __func__, hdmi->port_id,
 		hdmi->tmds_char_rate, rks->output_bpc, link_cfg->frl_enabled,
 		link_cfg->frl_rate_forced, link_cfg->frl_rate_per_lane, link_cfg->frl_lanes);
 }
 
-static int
-dw_hdmi_qp_rockchip_calc_frl_lane_cfg(struct rockchip_hdmi_qp *hdmi,
-				      unsigned long frl_rate,
-				      u8 *rate_per_lane, u8 *lanes)
-{
-	switch (frl_rate) {
-	case 48:
-		*rate_per_lane = 12;
-		*lanes = 4;
-		break;
-	case 40:
-		*rate_per_lane = 10;
-		*lanes = 4;
-		break;
-	case 32:
-		*rate_per_lane = 8;
-		*lanes = 4;
-		break;
-	case 24:
-		*rate_per_lane = 6;
-		*lanes = 4;
-		break;
-	case 18:
-		*rate_per_lane = 6;
-		*lanes = 3;
-		break;
-	case 9:
-		*rate_per_lane = 3;
-		*lanes = 3;
-		break;
-	default:
-		dev_err(hdmi->dev, "%s frl rate %lu is out of range, set to 40G\n",
-			__func__, frl_rate);
-
-		*rate_per_lane = 10;
-		*lanes = 4;
-		return -EINVAL;
-	}
-
-	return 0;
-}
-
 static int dw_hdmi_qp_rockchip_link_setup(struct rockchip_hdmi_qp *hdmi,
 					  struct rockchip_crtc_state *rks,
 					  struct dw_hdmi_qp_link_config *new_link_cfg,
@@ -264,7 +222,7 @@ static int dw_hdmi_qp_rockchip_link_setup(struct rockchip_hdmi_qp *hdmi,
 	rks->output_bpc = bpc;
 
 	dev_dbg(hdmi->dev,
-		"%s tmds=%llu bpc=%u frl_enabled=%d frl_rate_forced=%lu frl_rate_per_lane=%u frl_lanes=%u\n",
+		"%s tmds=%llu bpc=%u frl_enabled=%d frl_rate_forced=%u frl_rate_per_lane=%u frl_lanes=%u\n",
 		__func__, tmds_char_rate, bpc,
 		link_cfg->frl_enabled, link_cfg->frl_rate_forced,
 		link_cfg->frl_rate_per_lane, link_cfg->frl_lanes);
@@ -319,29 +277,23 @@ dw_hdmi_qp_rockchip_encoder_atomic_check(struct drm_encoder *encoder,
 
 	//TODO: sync impl with dw_hdmi_qp_rk3588_force_link_rate()
 	if (conn_state->hdmi.tmds_char_rate > HDMI20_MAX_TMDSRATE) {
-		link_cfg.frl_rate_per_lane = info->hdmi.max_frl_rate_per_lane;
-		link_cfg.frl_lanes = info->hdmi.max_lanes;
-		link_cfg.frl_rate_forced = hdmi->link_cfg.frl_rate_forced;
-
 		if (link_cfg.frl_rate_forced) {
-			u8 rate_per_lane, frl_lanes;
-			dw_hdmi_qp_rockchip_calc_frl_lane_cfg(hdmi, link_cfg.frl_rate_forced,
-							      &rate_per_lane, &frl_lanes);
-
-			if (link_cfg.frl_rate_per_lane > rate_per_lane)
-				link_cfg.frl_rate_per_lane = rate_per_lane;
-			if (link_cfg.frl_lanes > frl_lanes)
-				link_cfg.frl_lanes = frl_lanes;
+			if (link_cfg.frl_rate_per_lane > info->hdmi.max_frl_rate_per_lane)
+				link_cfg.frl_rate_per_lane = info->hdmi.max_frl_rate_per_lane;
+			if (link_cfg.frl_lanes > info->hdmi.max_lanes)
+				link_cfg.frl_lanes = info->hdmi.max_lanes;
+			link_cfg.frl_rate_forced = true;
 		} else {
-			if (link_cfg.frl_rate_per_lane > 12)
-				link_cfg.frl_rate_per_lane = 12;
-			if (link_cfg.frl_lanes > 4)
-				link_cfg.frl_lanes = 4;
+			link_cfg.frl_rate_per_lane = info->hdmi.max_frl_rate_per_lane;
+			link_cfg.frl_lanes = info->hdmi.max_lanes;
 		}
 
+		if (link_cfg.frl_rate_per_lane > 12)
+			link_cfg.frl_rate_per_lane = 12;
+		if (link_cfg.frl_lanes > 4)
+			link_cfg.frl_lanes = 4;
+
 		link_cfg.frl_enabled = true;
-	} else {
-		link_cfg.frl_enabled = false;
 	}
 
 	ingest_fmt = dw_hdmi_qp_rockchip_get_vop_format(encoder, conn_state);
@@ -370,8 +322,8 @@ dw_hdmi_qp_rockchip_encoder_atomic_check(struct drm_encoder *encoder,
 	}
 
 	return dw_hdmi_qp_rockchip_link_setup(hdmi, s, &link_cfg,
-					     conn_state->hdmi.tmds_char_rate,
-					     conn_state->hdmi.output_bpc);
+					      conn_state->hdmi.tmds_char_rate,
+					      conn_state->hdmi.output_bpc);
 }
 
 static const struct
@@ -430,16 +382,15 @@ dw_hdmi_qp_rk3588_get_link_cfg(struct dw_hdmi_qp *dw_hdmi, void *data)
 	return &hdmi->link_cfg;
 }
 
-static int dw_hdmi_qp_rk3588_force_link_rate(struct dw_hdmi_qp *dw_hdmi,
-					     void *data, unsigned long frl_rate)
+static int dw_hdmi_qp_rk3588_force_link_rate(struct dw_hdmi_qp *dw_hdmi, void *data,
+					     u8 frl_rate_per_lane, u8 frl_lanes)
 {
 	struct rockchip_hdmi_qp *hdmi = (struct rockchip_hdmi_qp *)data;
 	struct dw_hdmi_qp_link_config link_cfg = hdmi->link_cfg;
 	struct drm_crtc *crtc = hdmi->encoder.encoder.crtc;
 	struct rockchip_crtc_state *rks;
-	u8 rate_per_lane, frl_lanes;
 
-	if (!frl_rate)
+	if (!frl_rate_per_lane || !frl_lanes)
 		return 0;
 
 	if (!crtc || !crtc->state) {
@@ -447,21 +398,19 @@ static int dw_hdmi_qp_rk3588_force_link_rate(struct dw_hdmi_qp *dw_hdmi,
 		return -EINVAL;
 	}
 
-	dw_hdmi_qp_rockchip_calc_frl_lane_cfg(hdmi, frl_rate, &rate_per_lane,
-					      &frl_lanes);
-
-	if (link_cfg.frl_rate_per_lane > rate_per_lane)
-		link_cfg.frl_rate_per_lane = rate_per_lane;
+	if (link_cfg.frl_rate_per_lane > frl_rate_per_lane)
+		link_cfg.frl_rate_per_lane = frl_rate_per_lane;
 
 	if (link_cfg.frl_lanes > frl_lanes)
 		link_cfg.frl_lanes = frl_lanes;
 
-	link_cfg.frl_rate_forced = frl_rate;
+	link_cfg.frl_rate_forced = true;
 	link_cfg.frl_enabled = true;
 
 	rks = to_rockchip_crtc_state(crtc->state);
 
-	return dw_hdmi_qp_rockchip_link_setup(hdmi, rks, &link_cfg, 0, rks->output_bpc);
+	return dw_hdmi_qp_rockchip_link_setup(hdmi, rks, &link_cfg,
+					      hdmi->tmds_char_rate, rks->output_bpc);
 }
 
 static const struct dw_hdmi_qp_phy_ops rk3588_hdmi_phy_ops = {
diff --git a/include/drm/bridge/dw_hdmi_qp.h b/include/drm/bridge/dw_hdmi_qp.h
index 1f42ce4181..2bd4a442cd 100644
--- a/include/drm/bridge/dw_hdmi_qp.h
+++ b/include/drm/bridge/dw_hdmi_qp.h
@@ -16,7 +16,7 @@ struct platform_device;
 
 struct dw_hdmi_qp_link_config {
 	bool frl_enabled;
-	unsigned long frl_rate_forced;
+	bool frl_rate_forced;
 	u8 frl_rate_per_lane;
 	u8 frl_lanes;
 };
@@ -27,7 +27,8 @@ struct dw_hdmi_qp_phy_ops {
 	enum drm_connector_status (*read_hpd)(struct dw_hdmi_qp *hdmi, void *data);
 	void (*setup_hpd)(struct dw_hdmi_qp *hdmi, void *data);
 	struct dw_hdmi_qp_link_config *(*get_link_cfg)(struct dw_hdmi_qp *hdmi, void *data);
-	int (*force_link_rate)(struct dw_hdmi_qp *hdmi, void *data, unsigned long frl_rate);
+	int (*force_link_rate)(struct dw_hdmi_qp *hdmi, void *data,
+			       u8 frl_rate_per_lane, u8 frl_lanes);
 };
 
 struct dw_hdmi_qp_plat_data {
diff --git a/include/drm/display/drm_scdc.h b/include/drm/display/drm_scdc.h
index f5081ba5a8..3349a95ffb 100644
--- a/include/drm/display/drm_scdc.h
+++ b/include/drm/display/drm_scdc.h
@@ -52,6 +52,8 @@
 #define  SCDC_READ_REQUEST_ENABLE		BIT(0)
 
 #define SCDC_CONFIG_1				0x31
+#define  SCDC_FFE_LEVELS_MASK			GENMASK(7, 4)
+#define  SCDC_FRL_RATE_MASK			GENMASK(3, 0)
 #define  SCDC_FRL_RATE_DISABLE			0
 #define  SCDC_FRL_RATE_3GBPS_3LANE		1
 #define  SCDC_FRL_RATE_6GBPS_3LANE		2
diff --git a/include/drm/display/drm_scdc_helper.h b/include/drm/display/drm_scdc_helper.h
index 34600476a1..782f7729b6 100644
--- a/include/drm/display/drm_scdc_helper.h
+++ b/include/drm/display/drm_scdc_helper.h
@@ -77,4 +77,8 @@ bool drm_scdc_get_scrambling_status(struct drm_connector *connector);
 bool drm_scdc_set_scrambling(struct drm_connector *connector, bool enable);
 bool drm_scdc_set_high_tmds_clock_ratio(struct drm_connector *connector, bool set);
 
+bool drm_scdc_set_frl(struct drm_connector *connector,
+		      u8 rate_per_lane, u8 lanes, u8 max_ffe_level);
+bool drm_scdc_calc_lower_frl(u8 in_rate_per_lane, u8 in_lanes,
+			     u8 *out_rate_per_lane, u8 *out_lanes);
 #endif
-- 
2.53.0

