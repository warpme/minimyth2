diff -Naur mythtv-master-20191206-g0cf59a3-old/mythtv/configure mythtv-master-20191206-g0cf59a3-new/mythtv/configure
--- mythtv-master-20191206-g0cf59a3-old/mythtv/configure	2019-12-06 16:28:53.799999999 +0100
+++ mythtv-master-20191206-g0cf59a3-new/mythtv/configure	2019-12-06 16:37:38.956666646 +0100
@@ -121,6 +121,7 @@
   --disable-vbox           disable support for V@Box TV Gateway boxes
   --disable-ceton          disable support for Ceton cards
   --disable-v4l2           disable Video4Linux support
+  --enable-v4l2request     enable Video4Linux V4l2-request support
   --disable-ivtv           disable ivtv support (PVR-x50) req. v4l2 support
   --disable-hdpvr          disable HD-PVR support
   --disable-dvb            disable DVB support
@@ -1941,7 +1942,6 @@
     wma_freqs
     wmv2dsp
 "
-
 MYTHTV_CONFIG_LIST='
     backend
     bdjava
@@ -1978,6 +1978,7 @@
     taglib
     v4l2
     v4l2prime
+    v4l2request
     v4l1
     valgrind
     x11
@@ -2770,6 +2771,7 @@
 enable v4l1
 enable v4l2
 enable v4l2prime
+disable v4l2request
 enable x11
 enable xnvctrl
 enable xnvctrl_external
@@ -4726,6 +4728,7 @@
         disable v4l1
         disable v4l2
         disable v4l2prime
+        disable v4l2request
         disable x11
         # Workaround compile errors from missing u_int/uint def
         CFLAGS=`echo $CFLAGS | sed 's/-D_POSIX_C_SOURCE=200112//'`
@@ -4776,6 +4779,7 @@
         disable v4l1
         disable v4l2
         disable v4l2prime
+        disable v4l2request
         enable  windows
         disable x11
         disable qtdbus
@@ -7205,6 +7209,10 @@
     ffopts="$ffopts --enable-libdrm"
 fi
 
+if enabled v4l2request; then
+    ffopts="$ffopts --enable-v4l2_request --enable-libdrm --enable-libudev"
+fi
+
 ffmpeg_extra_cflags="$extra_cflags -w"
 
 ## Call FFmpeg configure here
@@ -7415,7 +7423,7 @@
   echo "NVDEC support             ${nvdec-no}"
 fi
   echo "DRM support               ${drm-no}"
-  echo "Video4Linux codecs        ${v4l2-no} (DRM ${v4l2prime-no})"
+  echo "Video4Linux codecs        ${v4l2-no} (DRM:${v4l2prime-no} v4l2-request:${v4l2request-no})"
   echo "MMAL decoder support      ${mmal-no}"
   echo "OpenGL support            ${opengl-no}"
   echo "OpenGL ES 2.0             ${opengles-no}"
