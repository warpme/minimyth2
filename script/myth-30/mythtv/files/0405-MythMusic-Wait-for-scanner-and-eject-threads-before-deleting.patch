diff -Naur mythtv-master-20190105-g3382e1a-old/mythplugins/mythmusic/mythmusic/cdrip.cpp mythtv-master-20190105-g3382e1a-new/mythplugins/mythmusic/mythmusic/cdrip.cpp
--- mythtv-master-20190105-g3382e1a-old/mythplugins/mythmusic/mythmusic/cdrip.cpp	2019-01-05 12:45:51.086666667 +0100
+++ mythtv-master-20190105-g3382e1a-new/mythplugins/mythmusic/mythmusic/cdrip.cpp	2019-01-05 12:45:52.233333333 +0100
@@ -592,6 +592,18 @@
     QString command = "rm -f " + GetConfDir() + "/tmp/RipTemp/*";
     myth_system(command);
 
+    if (m_scanThread)
+    {
+        m_scanThread->wait();
+        delete m_scanThread;
+    }
+
+    if (m_ejectThread)
+    {
+        m_ejectThread->wait();
+        delete m_ejectThread;
+    }
+
     if (m_decoder)
         delete m_decoder;
 
@@ -790,6 +802,7 @@
 
 void Ripper::ScanFinished()
 {
+    m_scanThread->wait();
     delete m_scanThread;
     m_scanThread = nullptr;
 
@@ -1196,6 +1209,7 @@
 
 void Ripper::EjectFinished()
 {
+    m_ejectThread->wait();
     delete m_ejectThread;
     m_ejectThread = nullptr;
 
