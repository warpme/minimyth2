diff -Naur mythtv-master-20190320-gb121bce-old/mythplugins/mythmusic/mythmusic/cdrip.cpp mythtv-master-20190320-gb121bce-new/mythplugins/mythmusic/mythmusic/cdrip.cpp
--- mythtv-master-20190320-gb121bce-old/mythplugins/mythmusic/mythmusic/cdrip.cpp	2019-03-20 22:46:27.790000000 +0100
+++ mythtv-master-20190320-gb121bce-new/mythplugins/mythmusic/mythmusic/cdrip.cpp	2019-03-20 22:46:29.190000000 +0100
@@ -749,11 +749,24 @@
 
 void Ripper::ScanFinished()
 {
+    m_scanThread->wait();
     delete m_scanThread;
     m_scanThread = nullptr;
 
     m_tracks->clear();
 
+    if (m_scanThread)
+    {
+        m_scanThread->wait();
+        delete m_scanThread;
+    }
+
+    if (m_ejectThread)
+    {
+        m_ejectThread->wait();
+        delete m_ejectThread;
+    }
+
     if (m_decoder)
     {
         MusicMetadata *metadata;
@@ -1153,6 +1166,7 @@
 
 void Ripper::EjectFinished()
 {
+    m_ejectThread->wait();
     delete m_ejectThread;
     m_ejectThread = nullptr;
 
