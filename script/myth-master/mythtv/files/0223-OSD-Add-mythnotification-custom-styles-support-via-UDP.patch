diff -Naur mythtv-master-20190328-gdcb0847-old/mythtv/libs/libmythui/mythnotificationcenter.cpp mythtv-master-20190328-gdcb0847-new/mythtv/libs/libmythui/mythnotificationcenter.cpp
--- mythtv-master-20190328-gdcb0847-old/mythtv/libs/libmythui/mythnotificationcenter.cpp	2019-03-29 08:40:53.430000001 +0100
+++ mythtv-master-20190328-gdcb0847-new/mythtv/libs/libmythui/mythnotificationcenter.cpp	2019-03-29 08:40:55.123333334 +0100
@@ -336,6 +336,9 @@
         // if not, default to the main one
         foundtheme = LoadWindowFromXML("notification-ui.xml", theme, this);
     }
+    if (!m_style.isEmpty())
+        LOG(VB_GENERAL, LOG_NOTICE, QString("Custom notification style request: '%1', will load: '%2'")
+            .arg(m_style).arg(theme_attempt));
 
     if (!foundtheme) // If we cannot load the theme for any reason ...
         return false;
@@ -582,6 +585,11 @@
     {
         m_extra = tmp;
     }
+    tmp = data["asst"];
+    if (!(tmp.isNull() && (m_update & kMetaData)))
+    {
+        m_style = tmp;
+    }
     // We need to re-run init
     m_refresh = true;
 }
@@ -1509,6 +1517,7 @@
                                                                  "MythTV") : origin;
     data["asal"] = detail;
     data["asfm"] = extra;
+    data["asst"] = style;
 
     if (type == MythNotification::Error   ||
         type == MythNotification::Warning ||
diff -Naur mythtv-master-20190328-gdcb0847-old/mythtv/libs/libmythui/mythudplistener.cpp mythtv-master-20190328-gdcb0847-new/mythtv/libs/libmythui/mythudplistener.cpp
--- mythtv-master-20190328-gdcb0847-old/mythtv/libs/libmythui/mythudplistener.cpp	2019-03-29 08:40:53.430000001 +0100
+++ mythtv-master-20190328-gdcb0847-new/mythtv/libs/libmythui/mythudplistener.cpp	2019-03-29 08:40:55.123333334 +0100
@@ -114,6 +114,8 @@
     bool error = false;
     int visibility = 0;
     QString type = "normal";
+    QString style = "";
+    MythNotification::Priority priority = MythNotification::kDefault;
 
     QDomNode n = docElem.firstChild();
     while (!n.isNull())
@@ -143,6 +145,8 @@
                 visibility = e.text().toUInt();
             else if (e.tagName() == "type")
                 type = e.text();
+            else if (notification && e.tagName() == "style")
+                style = e.text();
             else if (notification && e.tagName() == "progress")
             {
                 bool ok;
@@ -173,7 +177,7 @@
                              MythNotification::TypeFromString(type),
                              msg, origin, description, image, extra,
                              progress_text, progress, timeout, fullscreen,
-                             visibility);
+                             visibility, priority, style);
         }
         else
         {
