diff -Naur fakeroot-1.18.4-old/libfakeroot.c fakeroot-1.18.4-new/libfakeroot.c
--- fakeroot-1.18.4-old/libfakeroot.c	2021-02-14 17:27:44.303333134 +0100
+++ fakeroot-1.18.4-new/libfakeroot.c	2021-02-14 17:32:51.203333125 +0100
@@ -71,6 +71,16 @@
 #define SEND_GET_STAT64(a,b) send_get_stat64(a)
 #endif
 
+#ifndef _STAT_VER
+#if defined (__aarch64__)
+#define _STAT_VER 0
+#elif defined (__x86_64__)
+#define _STAT_VER 1
+#else
+#define _STAT_VER 3
+#endif
+#endif
+
 /*
    These INT_* (which stands for internal) macros should always be used when
    the fakeroot library owns the storage of the stat variable.
